annotated-doc==0.0.4
annotated-types==0.7.0
anyio==4.12.1
arcp==0.2.1
async-timeout==5.0.1
attrs==26.1.0
bcrypt==5.0.0
# blender-mcp moved to requirements-plugins.txt (PR 3.B audit B.7).
# The orchestrator does not import blender-mcp at runtime — see
# docs/PLUGINS.md. Its transitive deps (the supabase stack, pyiceberg,
# mmh3, pyroaring, StrEnum) are intentionally NOT pinned here — they
# resolve when an operator installs requirements-plugins.txt, keeping
# the base install lean.
cachetools==6.2.6
certifi==2026.1.4
cffi==2.0.0
charset-normalizer==3.4.4
click==8.3.1
cryptography==48.0.0
deprecation==2.1.0
fastapi==0.133.0
flatbuffers==25.12.19
fsspec==2026.3.0
greenlet==3.3.2
h11==0.16.0
h2==4.3.0
hpack==4.1.0
httpcore==1.0.9
httpx-sse==0.4.3
httpx==0.28.1
hyperframe==6.1.0
idna==3.11
iniconfig==2.3.0
invoke==2.2.1
Jinja2==3.1.6
jsonschema-specifications==2025.9.1
jsonschema==4.26.0
librt==0.9.0
markdown-it-py==4.0.0
MarkupSafe==3.0.3
mcp==1.27.0
mdurl==0.1.2
mpmath==1.3.0
msgpack==1.1.2
multidict==6.7.1
mypy==1.20.2
mypy_extensions==1.1.0
networkx==3.6.1
numpy==2.4.4
# Transitive of pymupdf-layout (PDF layout model); not imported directly.
onnxruntime==1.24.4
packaging==26.0
paramiko==4.0.0
pathspec==1.1.0
pluggy==1.6.0
prompt_toolkit==3.0.52
propcache==0.4.1
protobuf==6.33.6

# Phase 2.3 OpenTelemetry — tracing SDK + OTLP/gRPC exporter +
# auto-instrumentation for FastAPI and the requests library.
# All beta-channel (0.62b1) packages are pinned together; bump as a
# group when upgrading. Loads dormant — see core/otel.py:init_tracing.
opentelemetry-api==1.41.1
opentelemetry-sdk==1.41.1
opentelemetry-exporter-otlp-proto-common==1.41.1
opentelemetry-exporter-otlp-proto-grpc==1.41.1
opentelemetry-instrumentation==0.62b1
opentelemetry-instrumentation-asgi==0.62b1
opentelemetry-instrumentation-fastapi==0.62b1
opentelemetry-instrumentation-requests==0.62b1
opentelemetry-proto==1.41.1
opentelemetry-semantic-conventions==0.62b1
opentelemetry-util-http==0.62b1
grpcio==1.80.0
asgiref==3.11.1
googleapis-common-protos==1.75.0
wrapt==2.1.2
pycparser==3.0
pydantic-settings==2.13.1
pydantic==2.12.5
pydantic_core==2.41.5
Pygments==2.20.0
PyJWT==2.12.1
pymupdf-layout==1.27.2.2
pymupdf4llm==1.27.2.2
PyMuPDF==1.27.2.2
PyNaCl==1.6.2
pyparsing==3.3.2
pytest-asyncio==1.3.0
pytest-mock==3.15.1
pytest==9.0.3
python-dateutil==2.9.0.post0
python-dotenv==1.2.2
python-multipart==0.0.22
python-slugify==8.0.4
PyYAML==6.0.3
redis==7.2.0
referencing==0.37.0
requests==2.32.5
rich==14.3.3
rocrate==0.15.0
rpds-py==0.30.0
ruff==0.15.12
shellingham==1.5.4
six==1.17.0
SQLAlchemy==2.0.47
sse-starlette==3.3.4
starlette==0.52.1
strictyaml==1.7.3
# Transitive of pymupdf-layout via onnxruntime; not imported directly.
sympy==1.14.0
# Loosened from `==0.10.0` 2026-05-11 so the opt-in eval extras
# (deepeval 4.x needs `<0.10`) can install alongside the base. pymupdf4llm
# (the only base-requirements user of tabulate) is happy with 0.9+.
tabulate>=0.9,<0.11
tenacity==9.1.4
text-unidecode==1.3
tomli==2.4.1
typer==0.24.1
typing-inspection==0.4.2
typing_extensions==4.15.0
urllib3==2.6.3
uvicorn==0.41.0
wcwidth==0.6.0
websockets==15.0.1
yarl==1.23.0
zstandard==0.25.0
prefect>=3.0,<4

# Phase 1.4 — DVC on TrueNAS for data versioning. The [ssh] extra pulls in
# asyncssh / sshfs so DVC can push/pull through the dvc-orchestrator user
# on the TrueNAS pool over SSH (no CIFS dependency).
dvc[ssh]>=3.67,<4

# Phase 2.1 — Postgres durable system-of-record. SQLAlchemy 2.0.47 above
# is reused (sync engine). psycopg 3.x [binary] ships manylinux wheels so
# no compile is needed in the LXC; [pool] gives us connection pooling
# without a separate dependency. Alembic for migrations.
psycopg[binary,pool]>=3.2,<4
alembic>=1.13,<2

# Repo-screening spike (2026-05-11) — chonkie chunking primitive.
# Pure-Python base install (no torch/transformers in the default
# wheelset). Dormant by default; nothing in the orchestrator wires it
# in yet. See ``core/chunking.py``.
chonkie>=1.6,<2

# Repo-screening spike (2026-05-11) — deepeval LLM output evaluation
# is OPT-IN. Lives in ``requirements-eval.txt`` for the same reason
# SkyPilot does: heavy dep tree (openai, posthog, pyfiglet, sentry-sdk,
# pytest-xdist, aiohttp, ...) for a primitive that ships DORMANT and
# is never invoked inside a run loop. Operators who want the eval
# primitive run::
#
#     pip install -r requirements-eval.txt
#
# Without that, ``eval_pkg.scoring.is_enabled()`` reports False and
# every score_response() call returns a zero-score EvalScore with
# error=True. The orchestrator boots fine; unit tests pass (they mock
# OllamaModel). See ``eval_pkg/scoring.py``.

# Phase 2.5 — SkyPilot for cloud-burst GPU is OPT-IN. Lives in
# ``requirements-cloud.txt`` because its dep tree (boto3 / pandas /
# casbin / lxml / etc.) conflicts with several exact pins above
# (click, typer, uvicorn, rocrate, pyiceberg). Operators who want
# cloud-burst run::
#
#     pip install -r requirements-cloud.txt
#
# Without that, ``core/sky.py``'s lazy-import returns None and
# ``is_enabled()`` reports False — the orchestrator boots fine and
# every burst route returns 503.
